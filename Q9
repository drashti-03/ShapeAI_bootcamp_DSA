#include<bits/stdc++.h>
using namespace std;
int main(){

    int n;

    cin >> n;
    for(int b,y,i = 0; i < n; i++){
        cin >> b;
        if(b >= 38 && (y = (5 - (b % 5))) < 3){
        cout << b + y << endl;
        }
        else{
        cout << b << endl;
        }
    }
}
